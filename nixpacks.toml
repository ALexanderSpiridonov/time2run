[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmd = 'pip install -r requirements.txt'

[start]
cmd = 'python railway-start.py'

[variables]
PORT = '8080'

# Fallback - uncomment and set your values if Railway variables don't work
# TELEGRAM_BOT_TOKEN = "your_bot_token_here"
# TELEGRAM_CHAT_ID = "your_chat_id_here"
